get:
  $ref: "./pension_charges_retrieve.yaml"

  summary: Retrieve Pension Charges{{#maybeTestOnly "3.0 retrieve-pension-charges"}}{{/maybeTestOnly}}

  security:
    - User-Restricted:
        - read:self-assessment

put:
  $ref: "./pension_charges_create_amend.yaml"

  summary: Create and Amend Pension Charges{{#maybeTestOnly "3.0 create-amend-pension-charges"}}{{/maybeTestOnly}}

  security:
    - User-Restricted:
        - write:self-assessment

delete:
  $ref: "./pension_charges_delete.yaml"

  summary: Delete Pension Charges{{#maybeTestOnly "3.0 delete-pension-charges"}}{{/maybeTestOnly}}

  security:
    - User-Restricted:
        - write:self-assessment
